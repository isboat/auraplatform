import {useState} from 'react'; import {ArrowRight,CheckCircle2} from 'lucide-react'; import {useAuth} from '../context/AuthContext';
export default function AuthPage({register=false}){const {signIn}=useAuth();const [sent,setSent]=useState(false);const submit=e=>{e.preventDefault();const data=new FormData(e.currentTarget);if(register)setSent(true);else{signIn(data.get('email'));location.href='/'}};if(sent)return <div className="center-card success"><CheckCircle2/><h1>Check your inbox</h1><p>We sent you a verification link. Follow it to finish setting up your account.</p><a href="/signin" className="button primary">Continue to sign in</a></div>;return <div className="auth-wrap"><div className="auth-aside"><span className="eyebrow">Welcome to Aura</span><h1>{register?'Create. Share. Inspire.':'Welcome back.'}</h1><p>{register?'Join a community built around media that matters.':'Your next discovery is waiting.'}</p></div><form className="auth-form" onSubmit={submit}><span className="eyebrow">{register?'Get started':'Member access'}</span><h2>{register?'Create your account':'Sign in to Aura'}</h2>{register&&<label>Name<input name="name" required placeholder="Your name"/></label>}<label>Email address<input name="email" type="email" required placeholder="you@example.com"/></label><label>Password<input name="password" type="password" minLength="8" required placeholder="At least 8 characters"/></label><button className="button primary">{register?'Create account':'Sign in'}<ArrowRight size={17}/></button><p>{register?'Already a member?':'New to Aura?'} <a href={register?'/signin':'/register'}>{register?'Sign in':'Create an account'}</a></p></form></div>}

import {Search} from 'lucide-react'; import MediaCard from '../components/MediaCard'; import {demoMedia} from '../data/media';
export default function SearchPage(){const q=new URLSearchParams(location.search).get('tag')||'';const items=q?demoMedia.filter(x=>x.tags.some(t=>t.includes(q.toLowerCase()))||x.title.toLowerCase().includes(q.toLowerCase())):demoMedia;return <div className="page-wide"><div className="page-heading"><span className="eyebrow">Explore Aura</span><h1>{q?`Results for “${q}”`:'Search all media'}</h1><form className="big-search"><Search/><input name="tag" defaultValue={q} placeholder="Try nature, music, design…"/><button>Search</button></form></div><div className="media-grid search-results">{items.map(x=><MediaCard key={x.id} item={x}/>)}</div>{!items.length&&<p className="empty">No media matches that search yet.</p>}</div>}

import {useState} from 'react';import {Settings,Save} from 'lucide-react';
export default function AdminPage(){const [registration,setRegistration]=useState(true);const [uploads,setUploads]=useState(true);const [saved,setSaved]=useState(false);return <div className="page-narrow"><div className="page-heading"><span className="eyebrow">Administration</span><h1>Platform controls</h1><p>Control which creator features are currently available.</p></div><form className="admin-card" onSubmit={e=>{e.preventDefault();setSaved(true)}}><Settings/><label><span><strong>Account registration</strong><small>Allow new users to access the signup page.</small></span><input type="checkbox" checked={registration} onChange={e=>{setSaved(false);setRegistration(e.target.checked)}}/></label><label><span><strong>Media uploads</strong><small>Allow signed-in users to access the upload page.</small></span><input type="checkbox" checked={uploads} onChange={e=>{setSaved(false);setUploads(e.target.checked)}}/></label><button className="button primary"><Save size={17}/>Save configuration</button>{saved&&<p className="saved">Configuration saved.</p>}</form></div>}

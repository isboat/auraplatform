import {useState} from 'react';import {Eye,ThumbsUp,ThumbsDown,MessageCircle,Trash2} from 'lucide-react';import {demoMedia} from '../data/media';import {compact} from '../components/MediaCard';
export default function MyUploadsPage(){const [items,setItems]=useState(demoMedia.slice(0,3));return <div className="page-wide"><div className="page-heading inline"><div><span className="eyebrow">Creator studio</span><h1>My uploads</h1><p>Track performance and manage your media.</p></div><a className="button primary" href="/upload">Upload media</a></div><div className="uploads-table">{items.map((item,i)=><div className="upload-row" key={item.id}><div className={`upload-thumb ${item.color}`}></div><div className="upload-info"><span className="status">{i===1?'In review':'Approved'}</span><a href={`/media/${item.id}`}>{item.title}</a><small>{new Date(item.createdAt).toLocaleDateString()}</small></div><div className="upload-metrics"><span><Eye/>{compact(item.views)}<small>Views</small></span><span><ThumbsUp/>{compact(item.likes)}<small>Likes</small></span><span><ThumbsDown/>{item.dislikes}<small>Dislikes</small></span><span><MessageCircle/>{item.comments}<small>Comments</small></span></div><button className="delete" onClick={()=>setItems(items.filter(x=>x.id!==item.id))} aria-label={`Delete ${item.title}`}><Trash2/></button></div>)}</div></div>}

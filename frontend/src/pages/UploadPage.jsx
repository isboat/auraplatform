import {useState} from 'react'; import {UploadCloud,FileVideo,CheckCircle2,X} from 'lucide-react';
export default function UploadPage(){const [file,setFile]=useState(null);const [done,setDone]=useState(false);const [description,setDescription]=useState('');if(done)return <div className="center-card success"><CheckCircle2/><h1>Your media is under review.</h1><p>We’ll make it available as soon as the review is complete. You can follow its status from My Uploads.</p><a className="button primary" href="/my-uploads">View my uploads</a></div>;return <div className="page-narrow"><div className="page-heading"><span className="eyebrow">Creator studio</span><h1>Share something remarkable.</h1><p>Video, image, or audio — bring your work to Aura.</p></div><form className="upload-form" onSubmit={e=>{e.preventDefault();setDone(true)}}><label className="dropzone"><input type="file" accept="video/*,image/*,audio/*" onChange={e=>setFile(e.target.files[0])} required/>{file?<><FileVideo/><strong>{file.name}</strong><span>{(file.size/1048576).toFixed(1)} MB</span></>:<><UploadCloud/><strong>Drop your media here</strong><span>or click to browse · Large files upload in resilient parts</span></>}</label>{file&&<button type="button" className="remove-file" onClick={()=>setFile(null)}><X/>Remove</button>}<div className="form-grid"><label>Title <span>Optional</span><input placeholder="Untitled masterpiece"/></label><label className="full">Description <span>{description.length}/255</span><textarea maxLength="255" value={description} onChange={e=>setDescription(e.target.value)} placeholder="Tell people about this media…"/></label><label className="full">Tags <span>Separate with commas</span><input placeholder="travel, ambient, documentary"/></label></div><div className="responsibility"><strong>You are responsible for the media you upload.</strong><span>Only share work you own or have permission to use.</span></div><button className="button primary submit-upload">Upload for review <UploadCloud/></button></form></div>}
